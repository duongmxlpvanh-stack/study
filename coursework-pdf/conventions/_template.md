# <科目名称> 约定

> 新增科目：复制本文件为 `conventions/<subject>.md`，`<subject>` 就是调用脚本时 `--subject` 的值（用小写、连字符，例如 `linear-algebra`）。填好下面两个**标记块**即可，脚本靠这两对注释标记定位内容，注释行不要删改。其余正文是给人/Claude 看的说明，脚本不读取。

## 给生成模型的符号约定块

下面标记之间的内容会被**原样注入每一次 DeepSeek 调用**，用来保证多章节、多次调用之间记号一致。只写记号约定本身，越精炼越好。

<!-- SYMBOL_BLOCK_START -->
（在此填写本科目的 LaTeX 记号约定。例如：向量用 \mathbf{}，集合用花体 \mathcal{}，……）
<!-- SYMBOL_BLOCK_END -->

## 给生成模型的提示补充块

下面标记之间的内容也会注入每一次调用，用来传达本科目特有的出题/解答要求与禁忌（例如难度基调、必须/禁止使用的方法、是否允许作图等）。

<!-- PROMPT_EXTRA_START -->
（在此填写本科目特有的生成指令。）
<!-- PROMPT_EXTRA_END -->

## 章节清单（`--sections all` 的展开来源）

下面标记块列出本课程的完整章节（建议小节级，一行一节）。`--sections all`（或 `全部`）会**原样读取**它逐节出题——这是"生成全部章节"不漏章的唯一可靠来源，务必填全、填准。注释行不要删改。

<!-- SYLLABUS_START -->
（每行一节，例如：）
第一章某主题（含若干小节关键词）
第二章某主题
<!-- SYLLABUS_END -->
